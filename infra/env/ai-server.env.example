# AI Server container env example.
# Copy this to infra/env/ai-server.env on the OCI host and fill real secrets there.
# Do not commit real keys.

AI_SERVER_API_KEY=replace-with-ai-server-internal-key

# This belongs only to the AI Server container, not the Quiz backend.
OPENAI_API_KEY=replace-with-ai-provider-key
