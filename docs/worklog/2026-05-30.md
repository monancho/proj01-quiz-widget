# Worklog - 2026-05-30

## Session Goal

Align the existing frontend with KRDS visual conventions without adding new product features.

## Starting State

- Branch: `feature/rendering-security`.
- Existing PR scope: PR #6 frontend/admin UX refinement.
- User requested full frontend/design alignment with KRDS.

## KRDS Token Alignment

### Work Log

- Added `docs/todos/krds-frontend-alignment.md`.
- Added `krds-uiux@^1.0.5` to the client.
- Loaded KRDS design token CSS in `client/src/main.jsx`.
- Added app-level CSS variables mapped to KRDS color, surface, border, input, button, state, and focus tokens.
- Refreshed admin surfaces, buttons, inputs, stats, Slug Group rows, table, modal, utility modal, iframe preview, quiz card, choices, feedback, result, and banner styling using KRDS token values.
- Preserved the existing admin wireframe, left navigation rail, vertical Slug Group/Quiz layout, modals, drag reorder, and iframe utility behavior.
- Restored broken Korean UI labels in admin and iframe screens.
- Verified current local DB has a published `phase-5-verify-175513` Slug Group with 3 quizzes.

### Verification Log

- `npm.cmd install krds-uiux@^1.0.5`: completed with 0 vulnerabilities.
- `npm.cmd run build` in `client/`: passed.
- Headless Chrome CDP QA created and deleted temporary KRDS QA data.
- Headless Chrome CDP QA verified admin desktop `1366px` has no horizontal overflow.
- Headless Chrome CDP QA verified admin mobile `390px` has no horizontal overflow.
- Headless Chrome CDP QA verified embed light and dark routes at `390px` have no horizontal overflow.
- Headless Chrome CDP QA verified KRDS token CSS is loaded and app primary color resolves to `#256ef4`.
- Static search found no remaining known mojibake markers in `client/src`.

## KRDS UI Redesign Pass

### Goal

Make the frontend visibly closer to KRDS public-service UI patterns, not just token-aligned.

### Work Log

- Added `docs/todos/krds-ui-redesign-pass.md`.
- Changed the admin shell to use a white left navigation rail, official blue active state, flat masthead, square section panels, dense summary strip, and table-oriented Slug Group/Quiz areas.
- Changed admin forms, buttons, inputs, modals, table headers, status badges, and drag handles to feel more KRDS-like and less card/decorative.
- Changed the iframe quiz widget to use a quiz title band, square panel, accent question block, structured answer rows, and more official state surfaces.
- Preserved existing routes, API calls, drag reorder, modal flows, iframe utility behavior, and `theme=light|dark|system`.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Chrome CDP QA created and deleted temporary KRDS redesign QA data.
- Headless Chrome CDP QA verified admin desktop `1366px` has no horizontal overflow.
- Headless Chrome CDP QA verified admin mobile rendering has no horizontal overflow.
- Headless Chrome CDP QA verified embed light and dark routes have no horizontal overflow, transparent body background, square panels, the quiz title band, and a `4px` question accent border.

## KRDS Neutral Density Tuning

### Goal

Reduce strong blue visual dominance and make the widget/admin UI feel neutral enough to fit many Tistory skins.

### Work Log

- Added `docs/todos/krds-neutral-density-tuning.md`.
- Shifted default visual emphasis from KRDS blue to neutral KRDS gray/black.
- Reduced admin masthead, active navigation, section borders, and widget question accents from strong blue to neutral line-based emphasis.
- Tuned admin font sizes, stat strip, table padding, list row height, form controls, modal spacing, and action bars for denser scanning.
- Tuned iframe panel width, title band, question block, choice rows, feedback spacing, and action area for quieter embedding.
- Kept semantic success/error/warning colors only for actual state feedback.
- Replaced the CSS-generated Korean title text with escaped CSS content to avoid mojibake.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Chrome CDP QA created and deleted temporary KRDS neutral QA data.
- Headless Chrome CDP QA verified admin desktop and mobile rendering without horizontal overflow.
- Headless Chrome CDP QA verified iframe light and dark routes render without horizontal overflow.
- Headless Chrome CDP QA verified the app primary variable resolves to neutral `#131416`.
- Headless Chrome CDP QA verified iframe body background remains transparent.

## Handoff Notes

- These were presentation-layer changes; backend API behavior was not changed.
- Backend smoke tests were not rerun because no API-facing code changed.
- The widget should stay visually neutral so it can fit many Tistory skins.

## KRDS Service Design Polish

### Goal

Move beyond color neutralization and bring the existing admin and iframe UI closer to service-ready KRDS-like visual quality without changing product behavior.

### Research Notes

- KRDS guidance emphasizes inclusive UI/UX, consistent service standards, reusable components, and design tokens.
- KRDS design principles allow each service to optimize for its own context while keeping a shared, consistent experience.
- USWDS token guidance supports limiting visual decisions to tokenized color, spacing, typography, and surface values.
- USWDS theme color guidance notes that neutral base tones can be the predominant tone, with stronger colors used proportionally.

### Work Log

- Added `docs/todos/krds-service-design-polish.md`.
- Added a final service-polish CSS layer with neutral service tokens for surfaces, lines, text, shadow, radius, control height, and font sizing.
- Polished the admin shell, sidebar, topbar, filters, stats, Slug Group list, quiz table, drag affordance, modals, theme segmented control, and iframe utility preview.
- Polished the public iframe quiz panel, title band, question block, answer rows, feedback, result state, and optional banner.
- Kept public iframe outer backgrounds transparent and kept strong colors reserved for semantic states.
- Preserved existing React behavior, API calls, routes, admin wireframe, iframe theme modes, and copy/preview flows.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Local API and Vite dev server were started for browser QA.
- Headless Edge CDP QA verified admin desktop `1366px` and mobile `390px` rendering without horizontal overflow.
- Headless Edge CDP QA verified iframe light, dark, and system routes at `390px` without horizontal overflow.
- Headless Edge CDP QA verified public iframe body background remains transparent.
- Headless Edge CDP QA verified neutral service styling values including admin background `#f4f5f6`, primary action `#1e2124`, panel radius `6px`, and iframe choice row min-height `52px`.
- Temporary `krds-service-*` QA data was deleted after verification.

## Blog-Compatible Design Sub-Phase Planning

### Goal

Create a stable design strategy so future visual work is split into small design sub-phases, with a TODO written before each sub-phase starts.

### Work Log

- Added `docs/06-blog-embed-design-strategy.md`.
- Split future blog-compatibility design work into D0 through D5:
  - D0 blog compatibility audit.
  - D1 embed footprint reduction.
  - D2 skin-adaptive surface tuning.
  - D3 typography and reading rhythm.
  - D4 action and state neutrality.
  - D5 blog skin QA matrix.
- Added design sub-phase operating rules to `AGENTS.md`.
- Updated `docs/todos/README.md` with the `design-dN-short-name.md` naming convention.
- Prepared `docs/todos/design-d0-blog-compatibility-audit.md`.
- Prepared `docs/todos/design-d1-embed-footprint.md` as the likely next implementation TODO after D0.

### Verification Log

- No application code was changed.
- No build was required for this documentation-only planning pass.
- Working tree status will be checked before commit.

## Design D0 Blog Compatibility Audit

### Goal

Audit the current iframe widget against representative blog contexts before making more design changes.

### Work Log

- Confirmed `docs/todos/design-d0-blog-compatibility-audit.md` existed before starting.
- Created temporary `design-d0-*` QA data and deleted it after the audit.
- Tested six simulated blog contexts:
  - Minimal white article.
  - Off-white editorial article.
  - Dense text-heavy article.
  - Image-heavy magazine article.
  - Dark article.
  - Narrow mobile article.
- Confirmed no application code changes were made during D0.

### Findings

- No horizontal overflow was found in any simulated blog context.
- Public iframe body background remained transparent.
- The dominant compatibility issue was vertical footprint, not color.
- The copied iframe height was `720px`.
- Desktop iframe-height-to-article-width ratios ranged from `0.86` to `1.06`.
- Narrow mobile ratio was `1.99`, which is too dominant for an article body.
- Panel shadow, title band, and question block were acceptable but could be quieter.

### Decision

Proceed to D1 embed footprint reduction.

## Design D1 Embed Footprint Reduction

### Goal

Reduce the public iframe widget's visual and vertical footprint while preserving behavior, theme modes, transparent iframe backgrounds, Markdown, and KaTeX.

### Work Log

- Updated `docs/todos/design-d1-embed-footprint.md` with D0 findings before CSS work.
- Reduced copied iframe code height from `720px` to `620px`.
- Reduced public embed shell padding and spacing.
- Reduced public embed panel min-height, border/shadow weight, and surface strength.
- Reduced quiz card min-height, title band height, question block padding/weight, choice row height, feedback spacing, and action area spacing.
- Kept choice rows at `48px` minimum for touch usability.
- Preserved existing quiz behavior, routes, API calls, theme modes, and admin workflow.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge D1 QA verified no horizontal overflow across the same six simulated blog contexts.
- Desktop iframe-height-to-article-width ratios improved from `0.86~1.06` to `0.74~0.91`.
- Narrow mobile ratio improved from `1.99` to `1.71`.
- Direct iframe checks verified light, dark, and system themes at `390px` with transparent body backgrounds.
- Mobile internal values after D1: shell padding `6px`, panel min-height `456px`, title band `36px`, choice row min-height `48px`.
- Markdown and KaTeX QA verified `.katex` and `strong` output still render without horizontal overflow.
- Temporary `design-d1-*` and `design-d1-rich-*` QA data was deleted after verification.

## Design D2 Skin-Adaptive Surface Tuning

### Goal

Make the public iframe widget feel less like a detached external card and more like an article-native component across white, off-white, gray, and dark blog skins.

### Work Log

- Added `docs/todos/design-d2-skin-adaptive-surface.md` before implementation.
- Reduced public iframe panel border opacity and shadow strength.
- Made the light panel, title band, question block, choice rows, and optional banner more translucent.
- Tuned dark mode panel and internal surfaces with lower surface contrast and lighter borders.
- Preserved the D1 `620px` copied iframe height and footprint reductions.
- Restored correct/incorrect choice surfaces after D2 opacity rules initially overrode semantic states.
- Preserved quiz behavior, theme modes, API calls, admin workflow, and transparent public iframe body backgrounds.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge D2 QA verified white, off-white, gray, dark, and mobile white blog background simulations with no horizontal overflow.
- Headless Edge D2 QA verified light, dark, and system iframe routes at `390px`.
- Public iframe body background remained transparent.
- Light mobile surface values included panel background `rgba(255, 255, 255, 0.94)` and panel shadow `0 2px 8px rgba(15, 23, 42, 0.035)`.
- Dark mobile surface values included panel background `rgba(22, 24, 27, 0.92)` and panel shadow `0 2px 10px rgba(0, 0, 0, 0.14)`.
- Correct/incorrect states remained visible after selection in light and dark themes.
- Temporary `design-d2-*` QA data was deleted after verification.

## Design D3 Typography and Reading Rhythm

### Goal

Make public iframe question, choice, feedback, and result text feel more natural inside blog prose while preserving quiz clarity and accessibility.

### Work Log

- Added `docs/todos/design-d3-typography-rhythm.md` before CSS work.
- Tuned public iframe base font size and line-height.
- Reduced question heading dominance while keeping it readable.
- Tuned long Korean choice text rhythm and kept choice touch targets.
- Tuned rich text paragraph, list, inline code, and KaTeX spacing inside the iframe widget.
- Tuned feedback answer/explanation and result summary typography so the widget reads more like a compact article component.
- Preserved D1 iframe footprint, D2 skin-adaptive surfaces, public transparent backgrounds, theme modes, routes, API calls, and quiz behavior.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge D3 QA created and deleted temporary `design-d3-*` data.
- Mobile `390px` QA verified long Korean questions and long Korean choices with no horizontal overflow.
- Mobile `390px` QA verified Markdown lists, inline code, KaTeX, feedback explanations, and result summary typography.
- Light, dark, and system iframe routes had no horizontal overflow.
- Public iframe body background remained transparent.
- Choice touch target height remained at least `48px`.
- Observed mobile typography values included question `15.68px / 24.304px`, choice text `14.56px / 21.5488px`, feedback explanation `13.92px / 21.9936px`, and result title `17.92px / 26.5216px`.
- Cleanup check found `0` leftover `design-d3-*` Slug Groups.

## Design D4 Action and State Neutrality

### Goal

Keep iframe actions and answer states clear and accessible without making buttons, hover states, or correct/incorrect feedback visually dominate surrounding blog content.

### Work Log

- Added `docs/todos/design-d4-action-state-neutrality.md` before CSS work.
- Tuned public iframe primary and secondary action buttons to reduce filled-button dominance.
- Tuned disabled primary action state to read as quiet and unavailable.
- Tuned choice hover, keyboard focus, muted, correct, and incorrect states.
- Tuned feedback success/error surfaces and borders to keep semantic clarity with softer visual weight.
- Tuned dark and system dark action/state surfaces.
- Preserved D1 footprint, D2 skin-adaptive surfaces, D3 typography, public transparent backgrounds, theme modes, routes, API calls, and quiz behavior.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge D4 QA created and deleted temporary `design-d4-*` data.
- Mobile `390px` QA verified light, dark, and system iframe routes with no horizontal overflow.
- Public iframe body background remained transparent.
- Keyboard focus remained visible on choice rows and the result retry action.
- Disabled primary action rendered as a quiet neutral surface.
- Correct and incorrect choice states remained recognizable in light and dark themes.
- Feedback correct/incorrect boxes used quieter semantic borders and backgrounds.
- Result summary and retry action rendered correctly.
- A transition-aware follow-up check verified light correct state `rgba(240, 248, 243, 0.74)` and incorrect state `rgba(255, 244, 246, 0.76)` after CSS transitions completed.
- Cleanup checks found `0` leftover `design-d4-*` Slug Groups.

## Design D5 Blog Skin QA Matrix

### Goal

Verify the final D1-D4 public iframe design against representative blog contexts before deployment work, with the priority on blog compatibility rather than brand visibility.

### Work Log

- Added `docs/todos/design-d5-blog-skin-qa-matrix.md` before QA work.
- Created temporary published `design-d5-*` QA data with 3 quizzes.
- Ran six simulated parent blog contexts:
  - Minimal white article.
  - Off-white editorial article.
  - Dense text blog.
  - Image-heavy magazine article.
  - Dark article skin.
  - Narrow mobile article.
- Ran direct iframe checks for light, dark, and system themes at mobile width.
- Ran embedded parent-page iframe interaction checks for answer click, incorrect state, correct state, and all-quiz final result.
- No application code changes were required during D5.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge D5 QA verified all six simulated parent blog contexts had no horizontal overflow.
- Parent iframe height-to-article-width ratios:
  - Minimal white article: `0.82`.
  - Off-white editorial article: `0.91`.
  - Dense text blog: `0.97`.
  - Image-heavy magazine article: `0.82`.
  - Dark article skin: `0.91`.
  - Narrow mobile article: `1.71`.
- Direct iframe checks verified light, dark, and system routes at `390px` with no internal horizontal overflow.
- Public iframe body background remained transparent.
- Markdown, bold text, and KaTeX rendered in direct iframe checks.
- Light and dark correct/incorrect states and result views rendered during interaction checks.
- Cleanup check found `0` leftover `design-d5-*` Slug Groups.
- Embedded parent-page iframe interaction QA passed for light desktop, dark desktop, and system mobile contexts.
- Embedded iframe interaction QA verified answer click, incorrect state, correct state, all-quiz final result, no parent horizontal overflow, no iframe internal horizontal overflow, and transparent iframe body background.
- Cleanup check found `0` leftover `design-d5-embedded-*` Slug Groups.

## Design Follow-up Embed Action Spacing

### Goal

Make the public iframe quiz flow feel more natural around answer selection, feedback, and the next/result action button.

### Work Log

- Added `docs/todos/design-followup-embed-action-spacing.md` before CSS work.
- Measured the current mobile spacing around choices, feedback, and the next button.
- Found the pre-answer choice-to-action gap was about `97px` at mobile width because the quiz card kept a fixed minimum height.
- Tuned the public iframe quiz card to use natural height for the active question content.
- Removed the toolbar-like action area surface and top border in the public iframe.
- Centered the next/result action button so it feels connected to the quiz flow instead of pinned to the right edge.
- Preserved quiz behavior, scoring, theme modes, iframe height, transparent public iframe body background, and D1-D5 blog compatibility constraints.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge spacing QA created and deleted temporary `design-spacing-*` and `design-spacing-system-*` data.
- Mobile light route pre-answer choice-to-action gap improved from about `97px` to `0px`.
- Mobile light route incorrect-answer feedback-to-action gap was `0px`.
- Mobile light route correct-answer feedback-to-action gap was `0px`.
- Mobile light route final-question feedback-to-result-button gap was `0px`.
- Next/result action button was centered in light, dark, and system route checks.
- Light, dark, and system iframe routes had no horizontal overflow.
- Public iframe body background remained transparent.
- Result screen still rendered correctly.

## Design Follow-up Dark Mode and Result Density

### Goal

Check dark mode UI colors and reduce unnecessary final result height while preserving the existing iframe quiz behavior.

### Work Log

- Added `docs/todos/design-followup-dark-result-density.md` before CSS work.
- Tuned dark iframe panel, title band, question, choice, muted, correct, incorrect, feedback, result, and banner colors with a quieter neutral surface.
- Kept semantic correct/incorrect colors visible but less visually heavy in dark mode.
- Reduced the final result state by letting the result panel use natural height instead of the quiz-solving minimum height.
- Preserved quiz flow, scoring, retry behavior, theme modes, generated iframe height, and transparent public iframe body backgrounds.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge dark/result QA created and deleted temporary `design-dark-result-*` data.
- Dark route mobile `390px` verified disabled answer state, incorrect answer state, correct answer state, final result state, and centered retry action.
- System dark route mobile `390px` verified the same flow under dark color-scheme emulation.
- Dark and system dark result panel height measured `219px`, down from `456px` in the initial quiz state.
- Dark and system dark routes had no horizontal overflow.
- Public iframe body background remained transparent.

## Admin Markdown Authoring UX

### Goal

Improve Markdown/LaTeX authoring confidence by adding common textarea continuation shortcuts and verifying richer rendering scenarios.

### Work Log

- Added `docs/todos/admin-markdown-authoring-ux.md` before implementation.
- Added a small Markdown continuation helper inside the admin quiz modal flow.
- Wired the helper to the `question` and `explanation` textareas.
- Pressing Enter now continues unordered list prefixes: `-`, `*`, and `+`.
- Pressing Enter now continues numbered list prefixes such as `1.` and `1)`.
- Pressing Enter now continues blockquote prefixes such as `>`.
- Pressing Enter on an empty continuation line exits the current list or blockquote.
- Kept normal Enter behavior unchanged for plain text and modified Enter shortcuts.
- Preserved current Markdown/LaTeX renderer, live widget preview, API behavior, and public iframe quiz behavior.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge admin authoring QA created and deleted temporary `markdown-ux-*` data.
- Admin question textarea verified unordered continuation: `- alpha` to `- alpha\n- `.
- Admin question textarea verified empty unordered continuation exit: `- alpha\n- ` to `- alpha\n`.
- Admin question textarea verified numbered continuation: `1. alpha` to `1. alpha\n2. `.
- Admin explanation textarea verified blockquote continuation: `> quote` to `> quote\n> `.
- Admin explanation textarea verified empty blockquote exit: `> quote\n> ` to `> quote\n`.
- Admin widget preview rendered Markdown strong text, list items, and KaTeX.
- Public iframe mobile `390px` rendered Markdown strong text, list items, inline code, inline math, block math, and blockquote across question, choices, and explanation.
- Public iframe mobile check had no horizontal overflow and kept the body background transparent.

## AI Smell Review and First Remediation

### Goal

Use the user's AI-smell reference as a review lens, define staged review passes, analyze this project, and apply one low-risk behavior-preserving cleanup.

### Research Notes

- The user-provided article highlights recognizable LLM smells in repeated writing structures and generic web UI patterns.
- Supplemental code-smell research treats LLM-generated code quality as something that should be assessed beyond whether the code runs.
- For this project, the practical smell categories are overlayered CSS, oversized components, documentation bloat, mojibake, generic web patterns, unverified helper logic, and excessive generated reports.

### Work Log

- Added `docs/08-ai-smell-review-plan.md`.
- Added staged AI smell TODO files:
  - `docs/todos/ai-smell-r0-research.md`
  - `docs/todos/ai-smell-r1-analysis.md`
  - `docs/todos/ai-smell-r2-remediation-plan.md`
  - `docs/todos/ai-smell-r3-execution.md`
  - `docs/todos/ai-smell-r4-verification.md`
- Completed R0 research, R1 repository analysis, and R2 remediation planning.
- Identified high-risk smell candidates:
  - `client/src/styles.css` is large and has many chronological override layers.
  - `client/src/pages/AdminQuizManagerPage.jsx` is still large and mixes several concerns.
  - `docs/00-project-status.md` and latest worklog are useful but too long for quick handoff.
  - Several older docs/status entries contain mojibake.
- Chose the first execution target as extracting the Markdown authoring parser because it is isolated and low risk.
- Added `client/src/utils/markdownAuthoring.js`.
- Moved `getMarkdownContinuation` out of `AdminQuizManagerPage.jsx`.
- Kept the React event handler and caret restoration in the admin page.

### Verification Log

- Helper-level Markdown authoring checks passed for:
  - unordered list continuation.
  - unordered list exit.
  - ordered `1.` continuation.
  - ordered `1)` continuation.
  - blockquote continuation.
  - plain text no-op.
- `npm.cmd run build` in `client/`: passed.
- No backend/API behavior was changed.

## AI Smell Admin Embed Helper Extraction

### Goal

Continue reducing oversized admin page smell by extracting pure embed utility logic and shared option constants.

### Work Log

- Checked `client/src` for real mojibake markers.
- Confirmed current app source does not contain CJK mojibake or replacement characters; earlier garbled Korean was PowerShell output encoding.
- Added `docs/todos/ai-smell-r3b-admin-embed-helper-extraction.md`.
- Added `client/src/constants/adminOptions.js` for admin status and embed theme options.
- Added `client/src/utils/embedTools.js` for iframe URL/code generation.
- Removed the inline `buildEmbedUrl` and `buildIframeCode` helpers from `AdminQuizManagerPage.jsx`.
- Kept generated iframe height, theme behavior, URL shape, and admin modal workflow unchanged.

### Verification Log

- Helper-level embed checks passed for:
  - encoded post slugs.
  - dark theme URL generation.
  - invalid theme fallback to `system`.
  - iframe height `620`.
  - `allowtransparency`.
  - transparent iframe inline style.
- `npm.cmd run build` in `client/`: passed.
- No backend/API behavior was changed.

## AI Smell Admin Indicator Extraction

### Goal

Continue reducing oversized admin page smell by extracting small presentational indicators.

### Work Log

- Added `docs/todos/ai-smell-r3c-admin-indicator-extraction.md`.
- Added `client/src/components/admin/AdminIndicators.jsx`.
- Moved `LoadingRows`, `StatusBadge`, and `CompletionBadge` out of `AdminQuizManagerPage.jsx`.
- Preserved the existing CSS class names: `loading-block`, `status-badge`, and `completion-badge`.
- Kept admin behavior and status logic unchanged.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- No backend/API behavior was changed.

## UI/UX Component Review

### Goal

Review the current frontend UI/UX component by component without adding new product features.

### Work Log

- Added `docs/todos/uiux-component-review.md` before starting the review.
- Reviewed current component boundaries in `AdminQuizManagerPage.jsx`, `EmbedQuizPage.jsx`, quiz components, rich text, admin indicators, and `styles.css`.
- Checked admin shell, sidebar, topbar, filters, stats, Slug Group list, selected detail panel, quiz table, reorder affordance, Slug Group modal, quiz modal, iframe utility modal, public iframe shell, quiz card, choices, feedback, result, static banner, and rich text rendering.
- Added the human-facing report `docs/human/reports/2026-05-30-uiux-component-review.html`.
- Updated `docs/human/index.html` so the UI/UX review appears in the human documentation portal.

### Findings

- Admin desktop and mobile layouts do not horizontally overflow.
- Public iframe light and dark mobile routes do not horizontally overflow and keep the body background transparent.
- Public iframe answer flow works across initial, incorrect, correct, next, and final result states.
- The highest-priority UX follow-ups are quiz modal density, iframe utility modal density, admin mobile information density, and over-layered CSS structure.

### Verification Log

- API health check passed at `http://127.0.0.1:3000/health`.
- Vite admin route returned `200` at `http://127.0.0.1:5173/admin`.
- Headless Edge CDP checked admin desktop `1366px` and mobile `390px`.
- Headless Edge CDP checked public iframe `theme=light` and `theme=dark` at `390px`.
- Headless Edge CDP checked iframe initial, incorrect, correct, next, and final result states.
- `npm.cmd run build` in `client/`: passed.

### Handoff Notes

- No application behavior or styling code was changed in this review pass.
- Follow-up fixes should be split into separate TODOs before editing frontend code.

## UI/UX Parser Completion

### Goal

Finish the remaining UI/UX follow-up parsers from the component review without adding new product features.

### Work Log

- Added `docs/todos/uiux-parser-completion.md` before frontend edits.
- Added `client/src/styles/uiux-parser-completion.css` as a dedicated final UI/UX override layer.
- Imported the new CSS file after the existing KRDS tokens and base stylesheet in `client/src/main.jsx`.
- Reduced quiz modal density by using a two-column desktop layout, limiting preview height, and reducing preview choice row height.
- Reduced iframe utility modal density by lowering preview iframe height and tightening the code textarea area.
- Improved mobile admin density by tightening shell gaps, arranging topbar actions into two columns, expanding detail action tap areas, and hiding the answer column in the narrow quiz table.
- Kept public iframe behavior, theme modes, and transparent body background unchanged.

### Verification Log

- `npm.cmd run build` in `client/`: passed.
- Headless Edge CDP verified admin desktop `1366px` has no horizontal overflow.
- Headless Edge CDP verified admin mobile `390px` has no horizontal overflow.
- Headless Edge CDP measured quiz modal desktop height at about `667px`, down from the prior review's about `864px`.
- Headless Edge CDP measured iframe utility modal desktop height at about `756px`, with theme buttons, code textarea, and preview still available.
- Headless Edge CDP verified public iframe light mobile initial, incorrect, correct, next, and final result states.
- Headless Edge CDP verified public iframe dark mobile has no horizontal overflow and keeps transparent body background.

### Handoff Notes

- This pass intentionally avoids a broad rewrite of `client/src/styles.css`.
- The new CSS file is the rollback boundary for the UI/UX parser completion changes.

## Phase 8 Deployment Prep

### Goal

Prepare MVP deployment artifacts after PR #6 was reviewed and merged.

### Work Log

- Fast-forwarded local `develop` to `origin/develop` after PR #6 merge.
- Created `feature/docker-deploy` from updated `develop`.
- Added `docs/todos/phase-8-deployment-prep.md`.
- Added `.dockerignore`.
- Added `server/Dockerfile` using `node:24-slim`, production env defaults, migration-on-start, and `/data` SQLite path.
- Added `infra/docker-compose.yml` with API and Caddy reverse proxy services.
- Added `infra/caddy/Caddyfile` for reverse proxying to the API.
- Added `infra/env/api.env.example` for production backend environment values.
- Added `client/.env.example` for Vite/Cloudflare Pages values.
- Added Cloudflare Pages deployment guide, OCI backend checklist, deployment smoke test guide, and GitHub Actions backend deploy draft.
- Updated root `README.md` with deployment entry points.
- Added `docs/human/reports/2026-05-30-phase-8-deployment-prep.html`.
- Updated `docs/human/index.html` with the Phase 8 checkpoint.

### Verification Log

- `npm.cmd run smoke:phase2` in `server/`: passed after rerun outside sandbox because sandbox blocked temporary SQLite smoke DB creation.
- `npm.cmd run smoke:phase3` in `server/`: passed after rerun outside sandbox because sandbox blocked temporary SQLite smoke DB creation.
- `npm.cmd run smoke:phase4` in `server/`: passed after rerun outside sandbox because sandbox blocked temporary SQLite smoke DB creation.
- `npm.cmd run build` in `client/`: passed.
- `docker compose -f infra/docker-compose.yml config`: passed.
- Docker CLI and Compose CLI are installed, but a full image build was not run because the local Docker daemon was not running.

### Handoff Notes

- Actual production deployment still requires real Cloudflare Pages and OCI domains.
- Backend CORS must include the final Cloudflare Pages domain.
- If a real API domain is used directly through Caddy, update `infra/caddy/Caddyfile` from `:80` to the API domain.

## Deployment Domain Config

### Goal

Reflect the user's real deployment endpoints in deployment docs and environment examples.

### Provided Values

- OCI API endpoint: provided by user, then removed from committed docs/env examples to avoid exposing the raw server IP.
- Cloudflare Pages/frontend domain: `monancho.com`
- Tistory blog: `https://monancho.tistory.com/`

### Work Log

- Fast-forwarded local `develop` after PR #7 merge.
- Created `feature/deployment-domain-config`.
- Added `docs/todos/deployment-domain-config.md`.
- Updated `client/.env.example` with the production-style placeholder `VITE_API_BASE_URL=https://api.monancho.com` and Tistory banner URL.
- Updated `infra/env/api.env.example` with `CORS_ALLOWED_ORIGINS=https://monancho.com` and `TISTORY_HOME_URL=https://monancho.tistory.com/`.
- Updated Cloudflare Pages, OCI backend, and deployment smoke test docs with the current domains.
- Documented that `https://monancho.com` can be blocked from calling a raw HTTP server IP by browser mixed-content policy.
- Recommended moving production API calls to an HTTPS API domain such as `https://api.monancho.com`.

### Verification Log

- `docker compose -f infra/docker-compose.yml config`: passed.
- The provided HTTP API endpoint `/health` check timed out from the local environment.

### Privacy Follow-up

- Removed the raw server IP from committed docs and env examples.
- The real server IP should be stored only in local notes, DNS/OCI settings, or uncommitted production env files.
- No application runtime code was changed.
