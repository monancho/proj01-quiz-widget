# Worklog - 2026-06-10

## Session Goal

Add backend-only AI Server quiz generation integration for the Quiz backend.

## Starting State

- Branch: `feature/security-hardening`.
- AI Server image provided by user: `ghcr.io/monancho/ocl-ai-server:0.1.0`.
- AI Server docs live in `C:/dev_work/ocl_ai_server_01/docs/`.
- Existing Quiz backend has admin token protection, SQLite persistence, and manual quiz CRUD.

## Work Log

- Added `docs/todos/ai-quiz-generation-backend.md` before implementation.
- Added backend env loading for:
  - `AI_SERVER_BASE_URL`
  - `AI_SERVER_API_KEY`
  - `AI_SERVER_TIMEOUT_SECONDS`
- Updated local and OCI env examples with backend-only AI Server settings.
- Added `server/src/services/aiServerClient.js`.
- Implemented AI Server JSON wrapper parsing for `success=true` and `success=false`.
- Implemented `X-Internal-Api-Key` handling without logging or returning secrets.
- Implemented timeout, network error, non-2xx, invalid JSON, empty response, and AI error code mapping.
- Preserved `X-Request-Id` as response metadata for success and mapped errors.
- Added multipart image moderation client scaffolding, but did not expose it through Quiz backend routes.
- Added `server/src/routes/adminAiServer.routes.js` for admin-authenticated AI Server health/ready checks.
- Added `server/src/services/aiQuizGenerationService.js`.
- Added text, web, and YouTube AI quiz generation endpoints under Slug Group quiz routes.
- Kept AI generation limited to empty Slug Groups.
- Validated AI Server quiz output before saving:
  - exactly 3 questions
  - exactly 4 options per question
  - `answer_index === 0`
  - non-empty question and explanation text
- Stored generated quizzes in the existing schema with `answer_index: 0` mapped to `correctPosition: 1`.
- Returned `source`, `warning`, `usage`, `requestId`, and saved quiz `items` from generation endpoints.
- Added `server/src/scripts/smoke-ai-generation.js` with mock AI Server coverage.
- Added `smoke:ai-generation` to `server/package.json`.
- Added `ai-server` service image `ghcr.io/monancho/ocl-ai-server:0.1.0` to `infra/docker-compose.yml`.
- Added `infra/env/ai-server.env.example` for the AI Server container.
- Added gitignore protection for real `infra/env/*.env` files while keeping example files tracked.

## Verification Log

- `npm.cmd run smoke:ai-generation` in `server/`: passed.
- `npm.cmd run smoke:phase2` in `server/`: passed.
- `npm.cmd run smoke:phase3` in `server/`: passed.
- `npm.cmd run smoke:phase4` in `server/`: passed.
- `npm.cmd run smoke:security` in `server/`: passed.
- `docker compose -f infra/docker-compose.yml config`: passed.
- Live local AI Server `/health` at `http://localhost:8000`: returned `ok`.
- Live local AI Server `/ready` at `http://localhost:8000`: returned `ready`.
- Live AI Server text quiz generation returned 3 questions, 4 options on the first question, `answer_index = 0`, no warning, and `usage.input_chars = 145`.
- Live Quiz backend E2E used a temporary SQLite DB and temporary API port to call the local AI Server through admin routes.
- Live Quiz backend E2E verified admin AI health/ready proxy, text generation persistence of 3 quizzes, first quiz `correctPosition = 1`, request id presence, and non-empty Slug Group rejection with `AI_GENERATION_REQUIRES_EMPTY_SET`.
- Live AI Server invalid key check returned `401 INVALID_API_KEY`.
- Live AI Server short text check returned `400 SOURCE_TEXT_TOO_SHORT`.

## Handoff Notes

- The Quiz backend does not use `OPENAI_API_KEY`; that belongs only to the AI Server container.
- Local Quiz backend default AI URL is `http://localhost:8000`.
- OCI Compose backend AI URL is `http://ai-server:8000`.
- Real values for `ADMIN_API_TOKEN`, `AI_SERVER_API_KEY`, and AI Server provider keys must stay in uncommitted env files.
- No deployment, push, or PR was performed in this session.

## Admin UI Follow-up

- Added `docs/todos/ai-quiz-generation-admin-ui.md` before frontend implementation.
- Added Admin API helpers for text, web URL, and YouTube AI quiz generation.
- Added an `AI 생성` button to the Admin Slug Group detail quiz header.
- Kept AI generation available only for empty Slug Groups; non-empty groups show the disabled button with guidance.
- Added an AI generation modal with source type segmented control, difficulty selection, text/URL inputs, loading state, cancel, and submit.
- Added user-facing Korean messages for AI generation error codes.
- Added successful source warning display in the Slug Group detail area.
- Kept the frontend calling only Quiz backend Admin APIs.
- Did not add Doodle, image moderation, frontend AI Server secrets, source text logging, deployment, push, or PR work.

## Admin UI Verification Log

- `npm.cmd run build` in `client/`: passed.
- `npm.cmd run smoke:ai-generation` in `server/`: passed.
- Static frontend search found no `AI_SERVER_API_KEY`, Doodle integration string, or `console.log` in `client/src`.
- Browser UI verification was attempted, but the in-app browser runtime failed to start in this Windows sandbox session.

## Admin Frontend Auth Follow-up

- Removed the `/admin` frontend token input gate.
- Removed frontend admin token session storage usage.
- Stopped Admin API client from attaching `Authorization` headers from browser storage.
- Deleted the unused `client/src/utils/adminSession.js` helper.
- Changed the Admin topbar `Logout` action to a local view reset action.
- Updated deployment/security docs to state that admin secrets must not be entered into or stored by the frontend.
- Confirmed local `server/.env` does not define `ADMIN_API_TOKEN`, so local browser Admin calls can run tokenless.

## Admin Frontend Auth Verification Log

- `npm.cmd run build` in `client/`: passed.
- `npm.cmd run smoke:ai-generation` in `server/`: passed.
- `npm.cmd run smoke:phase2` in `server/`: passed.
- `npm.cmd run smoke:phase3` in `server/`: passed.
- `npm.cmd run smoke:phase4` in `server/`: passed.
- `npm.cmd run smoke:security` in `server/`: passed.
- Static frontend search found no admin token session, token gate, or Authorization header usage in `client/src`.

## Admin AI Loading UX Follow-up

- Added `docs/todos/admin-ai-generation-loading-ux.md` before implementation.
- Added source-type-specific waiting copy for text, web URL, and YouTube generation.
- Added an accessible in-modal loading panel with spinner, short explanation, and duplicate-submit reassurance.
- Changed the submit button label from `생성` to `생성 중` while a request is running.
- Kept the loading UI indeterminate because the backend does not stream progress.

## Admin AI Loading UX Verification Log

- `npm.cmd run build` in `client/`: passed.

## GitHub And Image Push

- Pushed `feature/security-hardening` to GitHub:
  - `fd15efc..656525a`
- Built backend Docker image from current workspace:
  - `ghcr.io/monancho/proj01-quiz-widget-api:develop`
  - `ghcr.io/monancho/proj01-quiz-widget-api:sha-656525a`
- Pushed both backend image tags to GHCR.
- Pushed image digest:
  - `sha256:b4f543ca169c3ab5886c1e98a4d459a966975055e25007fd75de39c111883358`
- No frontend secrets, server `.env`, or AI Server keys were committed or pushed.

## Branch Reconciliation

- Fetched latest remote branches with pruning.
- Merged `origin/feature/deployment-domain-config` into `feature/security-hardening` so the AI generation/security work includes the Cloudflare Workers frontend configuration update.
- Installed updated client dependencies after the Cloudflare Vite/Wrangler package changes.
- Added `.wrangler/` to `.gitignore` because the Cloudflare tooling creates it as a local generated directory.
- Prepared the reconciled branch for merge into `main` at the user's request.

## Branch Reconciliation Verification Log

- `npm.cmd install` in `client/`: passed, 0 vulnerabilities.
- `npm.cmd run build` in `client/`: passed.
- `npm.cmd run smoke:ai-generation` in `server/`: passed.
- `npm.cmd run smoke:phase2` in `server/`: passed.
- `npm.cmd run smoke:phase3` in `server/`: passed.
- `npm.cmd run smoke:phase4` in `server/`: passed.
- `npm.cmd run smoke:security` in `server/`: passed.
