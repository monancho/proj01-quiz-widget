# Worklog - 2026-05-29

## Session Goal

Prepare work tracking files, then implement Phase 0 and Phase 1 from `docs/03-task-breakdown.md`.

## Starting State

- Repository branch: `develop`.
- Remote: `origin` points to `https://github.com/monancho/proj01-quiz-widget.git`.
- Planning docs exist under `docs/`.
- No application source code exists yet.

## Phase 0 TODO

- [x] Create `client/`, `server/`, `migrations/`, `infra/`.
- [x] Add root `README.md`.
- [x] Review `.gitignore`.

## Phase 1 TODO

- [x] Add `server/package.json`.
- [x] Add Express app entrypoint.
- [x] Add `GET /health`.
- [x] Add environment variable loading.
- [x] Add `server/.env.example`.
- [x] Add SQLite connection helper.
- [x] Add migration SQL.
- [x] Add migration runner and `npm run db:migrate`.

## Work Log

- Created project tracking plan for Phase 0 + Phase 1.
- Created `feature/project-scaffold` branch from `develop`.
- Added root README and baseline project folders.
- Added Express API base under `server/`.
- Added `GET /health`.
- Added environment config helper and `server/.env.example`.
- Added SQLite connection helper using Node 24 `node:sqlite`.
- Added migration runner and initial `quiz_sets`/`quizzes` migration.
- Installed backend dependencies with `npm.cmd install`.
- Added root `AGENTS.md` as the persistent Codex operating guide.
- Added an initial Korean human-readable explanation of `AGENTS.md`.
- Updated project status with the agent-guide handoff structure.
- Replaced the Markdown human guide with a human-facing HTML guide.
- Updated `AGENTS.md` to clarify that human-facing reports are checkpoint documents, not routine session logs.
- Categorized human-facing docs into `index.html`, `guides/`, `reports/`, and `decisions/`.
- Moved the agent operation guide to `docs/human/guides/agent-operation-guide.html`.
- Added a Phase 0~1 checkpoint report under `docs/human/reports/`.
- Fast-forwarded local `develop` with Phase 0~1 work.
- Created `feature/server-api-mvp` for Phase 2+ backend API work.
- Added an `AGENTS.md` rule requiring phase TODO files before implementation.
- Added the Phase 2 TODO file under `docs/todos/`.
- Implemented Phase 2 Slug Group API.
- Added `post_slug` normalization and validation utilities.
- Added Slug Group repository/service/router layers.
- Added admin endpoints for list, create, detail, update, delete, and check-slug.
- Added `npm.cmd run smoke:phase2` API smoke test.
- Added Push and PR Strategy to `AGENTS.md`.
- Added explicit timing rules for when to update human-facing docs.
- Added the Phase 3 TODO file under `docs/todos/`.
- Implemented Phase 3 Quiz API.
- Added Quiz repository/service/router layers.
- Added nested quiz list/create endpoints under Slug Group routes.
- Added quiz detail/update/delete endpoints under `/api/admin/quizzes`.
- Added `npm.cmd run smoke:phase3` API smoke test.
- Added the Phase 4 TODO file under `docs/todos/`.
- Implemented Phase 4 Public Embed API.
- Added public embed repository/service/router layers.
- Added simple CORS middleware using `CORS_ALLOWED_ORIGINS`.
- Added `npm.cmd run smoke:phase4` API smoke test.
- Added API MVP human checkpoint report under `docs/human/reports/`.
- Expanded `docs/human/index.html` into a human-friendly development documentation portal.
- Pushed local `develop` to `origin/develop`.
- Pushed `feature/server-api-mvp` to origin with upstream tracking.
- Opened draft PR #1: `https://github.com/monancho/proj01-quiz-widget/pull/1`.
- Committed and pushed PR handoff status: `f6560ca docs: record github pr handoff`.
- Created local `feature/client-embed-mvp` branch for Phase 5 preparation.
- Added Phase 5 TODO file under `docs/todos/`.
- Implemented the Phase 5 React/Vite/Tailwind client scaffold.
- Added `/embed/:postSlug` route parsing.
- Added public embed API client.
- Added iframe quiz UI with loading, empty, error, active, completed, answer feedback, next, result, retry, and static banner states.
- Updated README with frontend commands and client environment variables.
- Pushed `feature/client-embed-mvp` to origin with upstream tracking.
- Opened stacked draft PR #2: `https://github.com/monancho/proj01-quiz-widget/pull/2`.
- Retried browser QA; the in-app Browser plugin still failed in this sandbox.
- Used headless Chrome CDP fallback to verify mobile viewport, desktop/mobile rendering, feedback, result, and retry flow.
- Fixed narrow-width iframe overflow by tightening panel, action row, banner, and heading CSS.
- Added Phase 5 human checkpoint report under `docs/human/reports/`.
- Fetched GitHub state and found PR #1 merged into `develop`.
- Found PR #2 merged into `feature/server-api-mvp`, so opened direct Phase 5 draft PR #3 into `develop`.
- Fetched GitHub state and found PR #3 merged into `develop`.
- Fast-forwarded local `develop` to `origin/develop`.
- Created `feature/admin-mvp` from updated `develop`.
- Added the Phase 6 TODO file under `docs/todos/`.
- Implemented Phase 6 admin API client.
- Added `/admin` route handling.
- Implemented admin Slug Group list, search, status filter, stats, create/edit/delete modal flows, quiz table, quiz create/edit/delete form, preview, iframe preview link, and iframe code copy fallback.
- Added explicit React imports for JSX files after dev runtime exposed `React is not defined` in Vite.
- Replaced blocking `window.prompt` clipboard fallback with an inline textarea fallback.
- Added Phase 6 human checkpoint report under `docs/human/reports/`.
- Committed Phase 6 implementation: `cc82950 phase6: implement admin frontend mvp`.
- Pushed `feature/admin-mvp` to origin with upstream tracking.
- Opened draft PR #4 into `develop`: `https://github.com/monancho/proj01-quiz-widget/pull/4`.
- Created `feature/rendering-security` as the Phase 7 planning branch stacked on `feature/admin-mvp`.
- Added the Phase 7 TODO file under `docs/todos/`.
- Installed Phase 7 rendering dependencies: `react-markdown`, `remark-gfm`, `remark-math`, `rehype-katex`, and `katex`.
- Added shared `RichText` renderer for safe Markdown/LaTeX rendering.
- Replaced public iframe question, choice, answer, and explanation text rendering with `RichText`.
- Replaced admin quiz table and quiz preview text rendering with `RichText`.
- Added route-level lazy loading for admin/embed pages so the admin page is not bundled into the first route chunk.
- Committed Phase 7 implementation: `e83b86e phase7: add safe markdown math rendering`.
- Pushed `feature/rendering-security` to origin with upstream tracking.
- Opened stacked draft PR #5 into `feature/admin-mvp`: `https://github.com/monancho/proj01-quiz-widget/pull/5`.
- Added a pre-deployment admin UX TODO file under `docs/todos/`.
- Added `draft` Slug Group status for incomplete groups.
- Added migration `0002_admin_status_and_reorder.sql`.
- Added quiz reorder API under Slug Group quiz routes.
- Changed quiz create/edit from inline lower panel to modal.
- Changed iframe preview and iframe code copy flows to modals.
- Removed manual `sort_order` from quiz create/edit; new quizzes use creation order.
- Added drag-and-drop quiz reordering in the admin quiz table.
- Committed admin UX refinement: `2c384f6 admin: refine predeploy quiz management ux`.
- Pushed `feature/rendering-security` after admin UX refinement.
- Found PR #4 had merged into `develop` and PR #5 had merged into `feature/admin-mvp`.
- Opened direct draft PR #6 into `develop`: `https://github.com/monancho/proj01-quiz-widget/pull/6`.
- Combined the separate iframe preview and iframe code copy actions into one utility modal.
- Updated the quiz create/edit modal preview to reuse the actual widget quiz card, including selectable answer feedback.
- Preserved one iframe utility action in the Slug Group detail header instead of separate preview/copy buttons.
- Committed and pushed admin UX follow-up: `59c9695 admin: unify embed and widget previews`.
- Added admin UX polish TODO: `docs/todos/admin-ux-polish-before-deploy.md`.
- Reduced status summary/filter UI to 4 visible categories: `전체`, `준비중`, `공개`, `비공개`.
- Changed incomplete Slug Groups to show a readonly automatic `준비중` state instead of a manual status selector.
- Changed quiz reordering to use a dedicated drag handle.
- Changed iframe code textarea to copy on click/focus without showing a routine success banner.
- Changed public embed shell/html/body backgrounds to transparent while keeping admin iframe preview visually framed.
- Committed and pushed admin UX polish: `1cfc53c admin: polish predeploy ux controls`.
- Pushed safety checkpoint tag before design experiment: `checkpoint-before-design-theme-20260529`.
- Reverted the unsupported compact functional UI experiment with `caec970` and `d4e13c1` so the existing admin wireframe is preserved.
- Added `docs/todos/embed-theme-only.md` to track the corrected scope: theme behavior only, no new unsupported product functions.
- Added iframe utility modal theme options for `시스템`, `화이트`, and `다크`.
- Applied the selected theme to the iframe preview URL, generated iframe code, copied code, and public embed route via `theme=light|dark|system`.
- Preserved transparent public iframe backgrounds while allowing the embed panel itself to switch light/dark/system styling.

## Verification Log

- `npm.cmd install`: completed with 0 vulnerabilities.
- `npm.cmd run db:migrate`: applied `0001_init_quiz_sets_and_quizzes.sql`.
- `/health` smoke test: returned `200` with `{"status":"ok","service":"proj01-quiz-widget-api",...}`.
- Implementation commit: `6a62601 phase0-1: scaffold backend foundation`.
- Agent guide commit: `45a44d2 docs: add agent operating guide`.
- Human docs restructure commit: `f5fb96f docs: separate human-facing reports`.
- Human docs category commit: `9d0ddda docs: categorize human documentation`.
- Phase TODO rule commit: `c5e79da docs: require phase todo before implementation`.
- `npm.cmd run db:migrate`: completed with 0 pending migrations.
- `GET /health`: returned `200`.
- `npm.cmd run smoke:phase2`: passed.
- Phase 2 implementation commit: `a78aa57 phase2: implement slug group api`.
- Push strategy commit: `9fa4daa docs: add push and pr strategy`.
- Human docs timing commit: `d6e17b2 docs: define human docs update timing`.
- Phase 3 TODO commit: `e194a8e docs: prepare phase 3 quiz api todo`.
- `npm.cmd run smoke:phase2`: passed after Phase 3 changes.
- `npm.cmd run smoke:phase3`: passed.
- Phase 3 implementation commit: `ebef1cc phase3: implement admin quiz api`.
- Phase 4 TODO commit: `b79532a docs: prepare phase 4 public embed api todo`.
- `npm.cmd run smoke:phase2`: passed after Phase 4 changes.
- `npm.cmd run smoke:phase3`: passed after Phase 4 changes.
- `npm.cmd run smoke:phase4`: passed.
- Phase 4 implementation commit: `079b8a6 phase4: implement public embed api`.
- API MVP human report commit: included in `079b8a6 phase4: implement public embed api`.
- Human docs portal commit: `5c843af docs: expand human docs portal`.
- Draft PR: `https://github.com/monancho/proj01-quiz-widget/pull/1`.
- PR handoff commit: `f6560ca docs: record github pr handoff`.
- Phase 5 TODO commit: `be405e3 docs: prepare phase 5 embed todo`.
- `npm.cmd install` in `client/`: completed with 0 vulnerabilities.
- `npm.cmd run build` in `client/`: passed.
- Verification API server on port `3100`: `/health` returned `200`.
- Verification Vite page: `/embed/phase-5-check-175528` returned `200`.
- Draft PR #2: `https://github.com/monancho/proj01-quiz-widget/pull/2`.
- `npm.cmd run build` after CSS fix: passed.
- Headless Chrome CDP mobile QA: `innerWidth = 390`, `scrollWidth = 390`, `overflow = false`.
- Headless Chrome CDP interaction QA: incorrect feedback, answer display, next flow, result summary, and retry reset verified.
- Draft PR #3: `https://github.com/monancho/proj01-quiz-widget/pull/3`.
- PR #3 is merged into `develop`.
- Phase 6 TODO commit: `b0b72d9 docs: prepare phase 6 admin todo`.
- `npm.cmd run build` after Phase 6 implementation: passed.
- Admin route `/admin`: returned `200`.
- Headless Chrome CDP admin QA created Slug Group `phase-6-admin-759869`, added 3 quizzes, published it, verified iframe preview URL, and verified copy fallback.
- Headless Chrome CDP edit/delete QA updated quiz 1, deleted and recreated quiz 3, and verified final `3/3` state.
- Headless Chrome CDP Slug Group delete QA created and deleted a throwaway Slug Group.
- Phase 6 implementation commit: `cc82950 phase6: implement admin frontend mvp`.
- Draft PR #4: `https://github.com/monancho/proj01-quiz-widget/pull/4`.
- Phase 7 TODO commit: `c497ea1 docs: prepare phase 7 rendering security todo`.
- `npm.cmd install react-markdown remark-gfm remark-math rehype-katex katex`: completed with 0 vulnerabilities.
- `npm.cmd run build` after Phase 7 implementation: passed.
- Phase 7 public iframe QA rendered Markdown and KaTeX, blocked unsafe content, and had no 390px mobile overflow.
- Phase 7 admin QA rendered Markdown and KaTeX in the quiz table through the shared renderer and blocked unsafe content.
- Phase 7 implementation commit: `e83b86e phase7: add safe markdown math rendering`.
- Stacked draft PR #5: `https://github.com/monancho/proj01-quiz-widget/pull/5`.
- `npm.cmd run smoke:phase2`: passed after admin UX refinement.
- `npm.cmd run smoke:phase3`: passed after admin UX refinement, including reorder API coverage.
- `npm.cmd run smoke:phase4`: passed after admin UX refinement.
- `npm.cmd run build` after admin UX refinement: passed.
- Headless Chrome admin UX QA verified iframe code modal, preview modal, quiz edit modal, drag reorder, and draft-only incomplete status options.
- Admin UX refinement commit: `2c384f6 admin: refine predeploy quiz management ux`.
- Direct draft PR #6: `https://github.com/monancho/proj01-quiz-widget/pull/6`.
- `npm.cmd run build` after unified iframe/widget preview follow-up: passed.
- Headless Chrome admin follow-up QA verified one iframe utility modal contains preview URL, iframe code textarea, and live iframe preview together.
- Headless Chrome admin follow-up QA verified the quiz modal shows the current quiz as the actual widget card and displays answer feedback after selecting a choice.
- Admin UX follow-up commit: `59c9695 admin: unify embed and widget previews`.
- `npm.cmd run build` after admin UX polish: passed.
- Headless Chrome admin UX polish QA verified 4 status cards/filter options, readonly automatic preparing state for incomplete groups, handle drag reorder, click-to-copy iframe code, no copy success banner, and no horizontal overflow.
- Headless Chrome embed background QA verified public embed `html`, `body`, and `.embed-shell` backgrounds are transparent.
- Admin UX polish commit: `1cfc53c admin: polish predeploy ux controls`.
- `npm.cmd run build` after reverting the unsupported compact design and applying theme-only behavior: passed.
- Headless Chrome theme-only QA verified original admin wireframe markers, no fake sidebar/notification UI, iframe modal theme options, `theme=dark` preview URL/code/clipboard behavior, light/dark/system embed rendering, no horizontal overflow, and transparent public embed backgrounds.

## Issues

- PowerShell blocks `npm.ps1`; use `npm.cmd` on Windows.
- First `npm.cmd install` attempt timed out in sandbox; rerun with network approval succeeded.
- First migration attempt could not create `data/` in sandbox; rerun with approval succeeded.
- Node 24 currently prints an experimental warning for `node:sqlite`.
- Phase 2 smoke test creates temporary SQLite runtime files and needed elevated execution in this sandbox.
- Phase 3 smoke test also creates temporary SQLite runtime files and needed elevated execution in this sandbox.
- Phase 4 smoke test also creates temporary SQLite runtime files and needed elevated execution in this sandbox.
- In-app Browser plugin verification failed because the node runtime could not start in this sandbox; headless Chrome CDP fallback was used for browser QA.

## Next Session Handoff

- Read `docs/00-project-status.md`.
- Read the latest file in `docs/worklog/`.
- Phase 0 + Phase 1 implementation is committed.
- Future sessions should start by reading `AGENTS.md`, `docs/00-project-status.md`, and the latest worklog.
- Phase 2 is implemented and verified.
- Phase 2 implementation is committed.
- `develop` and `feature/server-api-mvp` are pushed.
- PR #1 is merged into `develop`.
- Phase 3 TODO is prepared.
- Phase 3 is implemented and verified.
- Phase 3 implementation is committed.
- Phase 4 TODO is prepared.
- Phase 4 is implemented, verified, and committed.
- API MVP human checkpoint report is updated.
- Phase 5 TODO is prepared.
- Phase 5 client implementation is verified on `feature/client-embed-mvp`.
- PR #1 is merged into `develop`.
- PR #2 is merged into `feature/server-api-mvp`.
- PR #3 is merged into `develop`.
- Phase 6 TODO is prepared.
- Phase 6 admin frontend is implemented and verified on `feature/admin-mvp`.
- `feature/admin-mvp` is pushed to origin.
- Draft PR #4 is open into `develop`: `https://github.com/monancho/proj01-quiz-widget/pull/4`.
- `feature/rendering-security` exists locally for Phase 7 planning.
- Phase 7 TODO is prepared.
- Phase 7 rendering/security implementation is locally verified.
- `feature/rendering-security` is pushed to origin.
- PR #5 is merged into `feature/admin-mvp`: `https://github.com/monancho/proj01-quiz-widget/pull/5`.
- Pre-deployment admin UX refinement, unified iframe/widget preview follow-up, admin UX polish, revert of unsupported compact UI, and corrected iframe theme-only behavior are implemented and locally verified on `feature/rendering-security`.
- Direct draft PR #6 is open into `develop`: `https://github.com/monancho/proj01-quiz-widget/pull/6`.
- Latest follow-up commit `59c9695` is pushed to PR #6.
- Latest admin UX polish commit `1cfc53c` is pushed to PR #6.
- Revert commits `caec970` and `d4e13c1` and the corrected theme-only implementation are prepared for PR #6.
- After PR #6 merges, fast-forward local `develop` from `origin/develop`.
- Prepare Phase 8 TODO before Docker/deployment work.
